# create a new package
# from ros2_ws/src type:
ros2 pkg create --build-type ament_python --license Apache-2.0 --node-name my_package

# compilar desde cero
rm -rf build install log
colcon build --cmake-clean-cache
source install/setup.bash

# lanzar un nodo
ros2 run <package-name> <node-name>
